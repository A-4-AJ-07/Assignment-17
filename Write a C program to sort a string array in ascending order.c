#include<stdio.h>
#include<string.h>
int main()
{
	char str[10],temp;
	int i,j;
	printf("Enter the element of string:");
	fgets(str,10,stdin);
    
	puts(str);	
	for(i=0;str!='\0';i++)
	{

       
	}
	puts(str);
}